<!-- noet:begin -->
<!-- noet:managed-contract-version: 6 -->
Read the relevant `.noet/` Markdown records before choosing a direction.
Treat decisions/failures/constraints as accumulated judgment and continuity/attention as provisional.
Prefer `noet record decision` for decision lifecycle changes when the CLI is available; direct Markdown remains supported.
Maintain failures, constraints, continuity, and attention directly per `.noet/OPERATING.md`.
`noet sync --check` is read-only; `noet sync` may refresh only versioned/marked Noet-managed instruction surfaces.
Prefer `noet verify run` to execute declared verification commands; it executes checks, it does not prove them, and writes no evidence.
Do not run `apply`, `brief`, other `verify` subcommands, or legacy JSON-backed record writers merely because preserved implementation exists.
<!-- noet:end -->
